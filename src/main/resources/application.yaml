spring:
  profiles:
    active: dev
  application:
    name: parcial-arqsoft-back

server:
  port: 8080
  servlet:
    context-path: /api

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /docs
    tagsSorter: alpha